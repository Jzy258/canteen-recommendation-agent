# docker — 部署配置

**所有者：C（B 协助 ollama 服务）**

- 计划文件：`docker-compose.yml`（frontend + backend + ollama + mcp-weather）、`Dockerfile.backend`、`Dockerfile.frontend`
- 部署说明写进 `docs/DEPLOY.md`；ollama 挂载模型卷避免重复下载
