# 测试结果记录（v1.3.0 全量回归）

> 日期：2026-08-05　|　分支：v1.3.0　|　环境：Windows 10，Python 3.14.5（uv venv），Node 24.18.1
> 说明：pytest 收集时会误报脚本式测试（`tests/b/llm/test_llm_connectivity.py` 的 `url` 参数非 fixture、`tests/b/D3/test_record.py` 仅 `__main__` 重建临时库），故后两项按其设计以脚本方式运行验证。

---

## 1. 后端 B（Agent / LLM / 工具 / 中间件）— 106 passed

命令：`uv run pytest tests/b -q --ignore=tests/b/llm/test_llm_connectivity.py`

| 模块 | 覆盖内容 | 结果 |
|------|----------|------|
| `tests/b/D1` | 菜品检索 search、后端 /health、契约 | PASS |
| `tests/b/D2` | 推荐评分 scoring、理由生成 | PASS |
| `tests/b/D3` | 摄入记录 record（脚本方式 7/7） | PASS |
| `tests/b/D5` | Store 长期记忆、HITL 审批、中间件 Token | PASS |
| `tests/b/D6` | 天气 MCP、流式、会话隔离 | PASS |
| `tests/b/D7` | 边界数据、版本号 SemVer | PASS |
| `tests/b/D8` | 用户系统（JWT/PBKDF2）、管理员 API、数据隔离 | PASS |
| `tests/b/D9` | 克重调控（serving_grams / grams） | PASS |
| `tests/b/D10` | 历史对话持久化与恢复、定位 | PASS |
| 连通性 | `test_llm_connectivity.py`（需外部 LLM，脚本式） | 环境性跳过 |

## 2. 后端 A（数据 / 算法）— 关键模块通过

| 模块 | 覆盖内容 | 结果 |
|------|----------|------|
| `tests/a/D1~D3` | schema、种子数据 100 道菜、摄入记录联调 | PASS |
| `tests/a/D4` | 评分公式、组合优化（含偏好失效修复回归） | PASS |
| `tests/a/D5` | HITL 集成、边界测试数据（edge_scenarios/csv） | PASS |
| `tests/a/D6` | 天气推荐、测试隔离 | PASS |
| `tests/a/D8/D9` | 迁移（v11/v12）、克重 | PASS |

> 注：`tests/a/D5/test_acceptance.py` 等含 `sys.exit` 的脚本式测试以脚本运行；断言式用例全部通过。

## 3. 前端 Vitest — 30/30 通过

命令：`cd frontend && npm run test`

| 文件 | 用例数 | 结果 |
|------|--------|------|
| `src/api/chat.test.ts` | 流式 SSE 解析、session 透传、回退 | PASS |
| `src/views/ChatView.test.ts` | 空输入、流式逐字渲染、session 持久化、失败回退、双失败友好提示、停止中断、新会话清空 | PASS |
| `src/components/*.test.ts` | ProfileForm、DishCard 等组件 | PASS |
| `src/stores/*.test.ts` | chat/auth/profile store | PASS |
| 其他工具/类型测试 | parseDishes、analytics 等 | PASS |

> 修复记录：本次回归发现"今日吃什么？"快捷按钮（`el-button--primary`）早于发送按钮被 `find('button.el-button--primary')` 匹配导致 5 例点击错位，已改为精确定位发送按钮，恢复 30/30。

## 4. 端到端（人工）

| 场景 | 步骤 | 预期 | 结果 |
|------|------|------|------|
| E2E-1 问菜 | 聊天输入"红烧肉热量" | 返回营养信息+来源 | PASS（本地后端+云端 API） |
| E2E-2 预算推荐 | "10 元预算推荐" | 返回 ≤10 元荤素搭配+理由 | PASS |
| E2E-3 记录 HITL | "记录今天午餐吃红烧肉" | 生成待确认→确认后入账 | PASS |
| E2E-4 趋势 | 打开营养趋势页 | 热量/三大营养素图表正常 | PASS |
| E2E-5 登录隔离 | 登录 user1 与 user2 | 各自偏好/记录互不串 | PASS |

---

## 附：验收对照

- 核心流程用例 ≥ 5 组 ✅（查询/推荐/理由/记录/趋势/异常/登录）
- 覆盖边界与异常 ✅（空输入、超小预算、工具异常、流式失败回退、双失败提示、用户中止）
- 全部通过率：后端 106/106（脚本式模块各自 7/7 等）+ 前端 30/30
