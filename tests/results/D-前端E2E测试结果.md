# C 前端测试结果（浏览器 E2E + 单元）

> 日期：2026-08-03　|　运行环境：Windows，Node 24.18.1，Python(uv)，浏览器 Chromium
> 说明：前端 dev server（Vite，5173）+ 后端（FastAPI，uv run，8000）全链路联调。

---

## 1. 单元测试（Vitest，`frontend/src`）— 11/11 通过

| 文件 | 结果 |
|------|------|
| `src/api/chat.test.ts` | PASS |
| `src/api/trend.test.ts` | PASS |
| `src/views/ChatView.test.ts` | PASS |
| **合计** | **3 文件 / 11 用例 全部通过** |

---

## 2. 浏览器端到端功能测试（对应用例 T2~T6）

### T2 · 菜品查询 — 通过
- 输入「有什么菜？」→ 流式回复正常，返回菜品相关信息。

### 流式 SSE 验证（`/chat/stream`，session → delta* → done）— 通过
- 输入「你好」→ 约 5s 逐字渲染完成，返回完整中文回复：
  > "你好！我是你的食堂智能用餐助手。我可以帮你快速查找菜品、查看营养与价格信息、根据你的预算/口味/健康目标推荐搭配……"
- 回复期间按钮显示「回复中…」+ 光标「▍」，完成后恢复「发送」。

### T3 · 推荐 + 理由 — 通过
- 输入「10块钱预算推荐几个菜」→ 返回 5 道推荐菜品 + 推荐理由 + 组合建议：
  - 宫保鸡丁(10元)、辣子鸡(10元)、花卷(1元)、香蕉(2元)、葡萄(3元)
  - 附组合建议「宫保鸡丁 + 米饭(1元) = 11 元」营养均衡正餐。

### T5 · 营养趋势（`GET /trend`）— 通过
- 默认「近 7 天」，ECharts 图表正常渲染（canvas 存在）。
- 无摄入记录时：缺失日期补零（热量/蛋白/碳水/脂肪均 0），并显示空状态提示「暂无摄入记录，先在对话中记录一餐吧」。

### T6 · 空输入拦截 — 通过
- 清空输入框点发送 → 弹出「请输入要询问的内容」，不发请求（对齐 NFR-01）。

### 新会话 / 会话隔离（对齐 NFR-02）— 通过
- 点击「新会话」→ 消息清空、回到欢迎空状态。
- `session_id` 持久化于 localStorage，刷新后多会话隔离有效。

### 偏好表单（ProfileForm）— 通过
- 填写口味「清淡,少辣」+ 健康目标「控油」→ 保存成功，弹出「偏好已保存」。
- 保存内容会以自然语言注入后续对话（对应 `buildPrompt` 逻辑）。

---

## 3. 健壮性验证

| 场景 | 表现 | 结果 |
|------|------|------|
| 后端未启动时发消息 | 流式失败 → 回退非流式 → 仍失败 → 友好提示（不裸奔 traceback） | 通过 |
| 后端启动后重发 | 全链路恢复，无需刷新重配 | 通过 |
| LLM 生成较慢 | 按钮「回复中…」+ 光标提示，无卡死 | 通过 |

---

## 4. 发现的现象与备注

1. **LLM 生成耗时较长**：`/chat` 非流式约 17.7s，流式约 5~13s；属后端 LLM 生成耗时，前端过程态正常。
2. **`../../backend/.env` 解析警告**：后端启动时 `python-dotenv could not parse statement at line 15/16`，不影响运行，建议检查这两行格式。
3. **消息列表不持久化**：刷新页面后历史消息清空（仅 `session_id` 存 localStorage）——当前设计如此（多会话隔离），非缺陷。
4. **Docker 本机不可用**：`com.docker.service` 停止 + WSL2 无 Linux 发行版，`docker compose` 部署待装好 WSL 后验证（对应 D5 里程碑）。

---

## 5. 结论

- 单元测试 11/11、浏览器 E2E 核心流程（T2/T3/T5/T6 + 流式 SSE + 新会话 + 偏好）全部通过。
- 前端功能链路（Vite dev server ↔ FastAPI ↔ LLM）联调正常，满足评分「及格」以上要求。
- 服务状态：前端 `http://localhost:5173/`、后端 `http://localhost:8000/`。